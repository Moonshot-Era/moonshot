import { IconBrandApple } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Apple = (style: IconCommonProps) => (
  <IconBrandApple stroke={1.5} style={style} />
);
