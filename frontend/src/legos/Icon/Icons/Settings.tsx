import { IconSettings } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Settings = (style: IconCommonProps) => (
  <IconSettings stroke={1.5} style={style} />
);
