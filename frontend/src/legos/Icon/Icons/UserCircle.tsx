import { IconUserCircle } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const UserCircle = (style: IconCommonProps) => (
  <IconUserCircle stroke={2} style={style} />
);
