import { IconTransfer } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Transfer = (style: IconCommonProps) => (
  <IconTransfer stroke={1.5} style={style} />
);
