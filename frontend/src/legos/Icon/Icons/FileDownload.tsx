import { IconFileDownload } from "@tabler/icons-react";

import { IconCommonProps } from "../types";

export const FileDownload = (style: IconCommonProps) => (
  <IconFileDownload stroke={1.5} style={style} />
);
