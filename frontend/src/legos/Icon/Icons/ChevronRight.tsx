import { IconChevronRight } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const ChevronRight = (style: IconCommonProps) => (
  <IconChevronRight stroke={1.5} style={style} />
);
