import { IconTrendingDown } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const TrendingDown = (style: IconCommonProps) => (
  <IconTrendingDown stroke={1.5} style={style} />
);
