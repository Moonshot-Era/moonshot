import { IconDeviceImac } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Monitor = (style: IconCommonProps) => (
  <IconDeviceImac stroke={1.5} style={style} />
);
