import { IconSelector } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Selector = (style: IconCommonProps) => (
  <IconSelector stroke={1.5} style={style} />
);
