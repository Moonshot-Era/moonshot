import { IconBrandGoogle } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const Google = (style: IconCommonProps) => (
  <IconBrandGoogle stroke={1.5} style={style} />
);
