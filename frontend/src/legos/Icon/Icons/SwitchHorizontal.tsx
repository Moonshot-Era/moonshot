import { IconSwitchHorizontal } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const SwitchHorizontal = (style: IconCommonProps) => (
  <IconSwitchHorizontal stroke={2} style={style} />
);
