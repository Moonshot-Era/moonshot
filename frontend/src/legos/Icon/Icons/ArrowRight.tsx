import { IconArrowRight } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const ArrowRight = (style: IconCommonProps) => (
  <IconArrowRight stroke={1.5} style={style} />
);
