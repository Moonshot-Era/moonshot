import { IconAlertTriangleFilled } from '@tabler/icons-react';

import { IconCommonProps } from '../types';

export const AlertTriangle = (style: IconCommonProps) => (
  <IconAlertTriangleFilled color="#FF3D71" style={style} />
);
